<h1>Mis Calendarios</h1>

<%= link_to "← Volver al Dashboard", doctors_dashboard_path %>

<%= link_to "Nuevo Calendario", new_doctors_calendar_path %>

<ul>
  <% @calendars.each do |calendar| %>
    <li>
      <%= link_to l(calendar.date, format: :long), doctors_calendar_path(calendar) %>
    </li>
  <% end %>
</ul>


<div class="container py-4 py-lg-5">
  <div class="row align-items-center g-4 mb-4">
    <div class="col-lg-7">
      <div class="p-4 p-lg-5 rounded-4 shadow-sm" style="background: linear-gradient(135deg, #D9D8E0 0%, #B69CFF 35%, #F8F7F9 100%);">
        <p class="text-uppercase fw-semibold small mb-2 text-dark">Cuidado confiable</p>
        <h1 class="fw-bold mb-3">Tus citas, a un clic de distancia</h1>
        <p class="text-muted mb-4">Revisa y gestiona tus citas programadas. Confirma, reprograma o cancela con una experiencia simple y humana.</p>
        <div class="d-flex flex-wrap gap-2">
          <%= link_to 'Agenda tu cita ahora', new_doctors_appointment_path, class: "btn btn-primary fw-semibold px-4 py-2 shadow-sm" rescue nil %>
          <%= link_to 'Ver disponibilidad', doctors_appointments_path, class: "btn btn-outline-light text-dark fw-semibold px-4 py-2" %>
        </div>
      </div>
    </div>
    <div class="col-lg-5">
      <div class="row g-3">
        <div class="col-12">
          <div class="card shadow-sm border-0 rounded-4">
            <div class="card-body d-flex justify-content-between align-items-center">
              <div>
                <p class="text-muted mb-1">Citas agendadas</p>
                <h4 class="fw-bold mb-0"><%= @appointments.size %></h4>
              </div>
              <span class="badge text-bg-primary px-3 py-2">Hoy</span>
            </div>
          </div>
        </div>
        <div class="col-6">
          <div class="card shadow-sm border-0 rounded-4">
            <div class="card-body">
              <p class="text-muted mb-1">Pendientes</p>
              <h5 class="fw-bold mb-0"><%= @appointments.select { |a| a.status == "pendiente" }.count %></h5>
            </div>
          </div>
        </div>
        <div class="col-6">
          <div class="card shadow-sm border-0 rounded-4">
            <div class="card-body">
              <p class="text-muted mb-1">Completadas</p>
              <h5 class="fw-bold mb-0"><%= @appointments.select { |a| a.status == "completado" }.count %></h5>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="card shadow-sm border-0 rounded-4">
    <div class="card-body p-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
          <p class="text-uppercase fw-semibold small mb-1 text-muted">Lista de citas</p>
          <h4 class="mb-0">Citas asignadas</h4>
        </div>
        <div class="d-flex gap-2">
          <%= link_to 'Crear cita', new_doctors_appointment_path, class: "btn btn-primary fw-semibold px-3" rescue nil %>
          <%= link_to 'Actualizar', doctors_appointments_path, class: "btn btn-outline-secondary" %>
        </div>
      </div>

      <% if @appointments.any? %>
        <div class="row g-3">
          <% @appointments.each do |appt| %>
            <% status_class = case appt.status
                             when "completado" then "success"
                             when "cancelado" then "danger"
                             else "warning"
                             end %>
            <% patient_name = appt.patient&.user&.name || appt.patient&.id || "Paciente sin nombre" %>
            <div class="col-12">
              <div class="card border-0 shadow-sm rounded-4 h-100">
                <div class="card-body">
                  <div class="d-flex justify-content-between align-items-start mb-2">
                    <div>
                      <p class="text-muted mb-1 small">Fecha y hora</p>
                      <h5 class="fw-bold mb-0">
                        <%= l(appt.date, format: :long) rescue appt.date %> · <%= appt.hour.strftime("%H:%M") rescue appt.hour %>
                      </h5>
                    </div>
                    <span class="badge text-bg-<%= status_class %> px-3 py-2 text-capitalize"><%= appt.status %></span>
                  </div>
                  <div class="row g-2 mb-3">
                    <div class="col-md-6">
                      <p class="text-muted mb-1 small">Paciente</p>
                      <p class="mb-0 fw-semibold"><%= patient_name %></p>
                    </div>
                    <div class="col-md-6">
                      <p class="text-muted mb-1 small">Motivo</p>
                      <p class="mb-0"><%= appt.reason_for_consultation.presence || "Sin motivo registrado" %></p>
                    </div>
                  </div>
                  <div class="d-flex flex-wrap gap-2">
                    <%= link_to 'Ver detalle', doctors_appointment_path(appt), class: "btn btn-outline-secondary btn-sm" %>
                    <%= link_to 'Editar', edit_doctors_appointment_path(appt), class: "btn btn-primary btn-sm" %>
                    <%= link_to 'Confirmar', confirm_doctors_appointment_path(appt), method: :patch, class: "btn btn-success btn-sm" %>
                    <%= link_to 'Cancelar', cancel_doctors_appointment_path(appt), method: :patch, class: "btn btn-warning btn-sm" %>
                    <%= link_to 'Eliminar', doctors_appointment_path(appt), method: :delete, data: { confirm: '¿Eliminar esta cita?' }, class: "btn btn-danger btn-sm" %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-5">
          <p class="text-muted mb-3">No hay citas asignadas todavía.</p>
          <%= link_to 'Crear la primera cita', new_doctors_appointment_path, class: "btn btn-primary px-4" rescue nil %>
        </div>
      <% end %>
    </div>
  </div>
</div>

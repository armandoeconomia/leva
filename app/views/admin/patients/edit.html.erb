<% content_for :page_title do %>
  Recepción · Editar Paciente
<% end %>

<div class="panel-page-header">
  <div>
    <span class="panel-eyebrow"><i class="fa fa-user-injured"></i> Pacientes</span>
    <h1 class="panel-page-title">Editar paciente</h1>
    <p class="panel-page-description">Actualiza contactos, seguros y antecedentes para mantener la continuidad del servicio.</p>
  </div>
  <div class="d-flex gap-2">
    <%= link_to "Ver detalle", admin_patient_path(@patient), class: "btn-panel-ghost" %>
  </div>
</div>

<%= render "form" %>

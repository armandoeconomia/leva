<% content_for :page_title do %>
  Recepción · Editar Cita
<% end %>

<div class="panel-page-header">
  <div>
    <span class="panel-eyebrow"><i class="fa fa-calendar-check"></i> Citas</span>
    <h1 class="panel-page-title">Editar cita</h1>
    <p class="panel-page-description">Ajusta horarios, motivos o estados para mantener informados a pacientes y especialistas.</p>
  </div>
  <div class="d-flex gap-2">
    <%= link_to "Ver detalle", admin_appointment_path(@appointment), class: "btn-panel-ghost" %>
  </div>
</div>

<%= render "form" %>
